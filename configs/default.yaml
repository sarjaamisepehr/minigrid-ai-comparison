seed: 42
device: "cuda"  # or "cpu"

environment:
  name: "minigrid_8x8"
  max_steps: 100

training:
  total_episodes: 10000
  eval_frequency: 100
  checkpoint_frequency: 500
  batch_size: 64
  learning_rate: 0.001

logging:
  log_dir: "logs"
  use_tensorboard: true
  verbose: true